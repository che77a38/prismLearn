<Page x:Class="testDialog.Views.MsgView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:testDialog.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="{Binding Title}">

    <StackPanel Orientation="Vertical">
        <TextBlock Text="编辑窗口" FontSize="28" Width="auto" Height="auto"/>
        <TextBox Width="300"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
            <Button Content="确定" Width="100" Height="auto" Margin="5" Command="{Binding OkCommand}"/>
            <Button Content="取消" Width="100" Height="auto" Margin="5" Command="{Binding CancleCommand}"/>
        </StackPanel>
    </StackPanel>
</Page>
